buildscript {
    repositories {
        jcenter()
    }
    dependencies {
        classpath 'com.android.tools.build:gradle:+'
        classpath 'com.jakewharton.sdkmanager:gradle-plugin:+'
    }
}

allprojects {
    repositories {
        jcenter()
    }
}

task('uploadArchives') {
}
